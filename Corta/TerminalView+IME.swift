import AppKit

/// IME composition (Track A, M3.1–M3.4): `NSTextInputClient` conformance,
/// marked-text (preedit) state, and `firstRectForCharacterRange:` live here.
/// Empty until Track A lands; the file exists so the track owns it outright.
extension TerminalView {
}
