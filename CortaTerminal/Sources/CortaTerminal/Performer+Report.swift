extension Performer {
    // Query responses — DA1, DA2 and DSR cursor-position reports land here in
    // M2.2. Responses are fixed-format: no text from the input stream is ever
    // echoed back (SECURITY.md §2.2). Empty until then.
}
